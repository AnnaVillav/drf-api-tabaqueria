[project]
name = "drf-api-tabaqueria"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "AnnaVillav", email = "anabellavillaverde1990@gmail.com" }
]
requires-python = ">=3.12"
dependencies = [
    "django>=6.1",
    "djangorestframework>=3.18.0",
]

[project.scripts]
drf-api-tabaqueria = "drf_api_tabaqueria:main"

[build-system]
requires = ["uv_build>=0.12.7,<0.13.0"]
build-backend = "uv_build"
